# C-522 M0 spike report (exp verification)

- Deps: @spatialdata/core@0.10.0, @spatialdata/vis@0.10.0, anndata.js@0.0.2, zarrita@0.7.5 installed with --legacy-peer-deps (peer conflict with @vivjs/loaders -> zarrita 0.5.4). npm nests 0.5.4 under @vivjs/loaders and anndata.js; 0.7.5 satisfies @spatialdata/*, zarrextra, ol. Both copies coexist; viv still loads.
- Build: `npm run build` green with deps present. Typecheck: 65 pre-existing errors on origin/main (testing-library typings), none introduced by the new deps.
- Fixture: blobs.sdata.zarr generated locally with spatialdata 0.8.0 (uv, python 3.12): images (2 incl. multiscale), points, labels (2), shapes (circles/multipolygons/polygons), one table keyed region=blobs_labels.
- readZarr via FetchStore (zarrita 0.7 RemoteStore equivalent): enumerates all elements + coordinate systems; getAssociatedTable works with BARE element keys (qualified "labels/blobs_labels" returns none — use bare keys).
- @spatialdata/vis exports confirmed: SpatialCanvas, SpatialCanvasViewer, SpatialCanvasProvider, SpatialViewer, SpatialLayer, composeSpatialDeckLayers, ImageLayerContextProvider.
- Node-only limitation hit: shapes polygon tessellation pulls parquet-wasm inline WASM loader that fails under plain node ESM (expected — browser path is the supported one; the package also exposes ./parquet-wasm and ./parquet-worker subpaths for bundler wiring).
- Next: browser verification inside the app (M2 skeleton) — Vite is the supported bundler path for the worker subpaths.
