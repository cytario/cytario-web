# Test Fixtures

This directory hosts shared assets for Vitest suites.

## Images

- `images/aware.jp2` – Minimal placeholder payload used by the JPEG2000 decoding tests. The file only needs to provide deterministic bytes for the unit tests and is **not** a production-quality image.
