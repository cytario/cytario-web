import { MAX_CHANNELS } from "@hms-dbmi/viv";

/** viv's rendering cap on simultaneously composited channels (see MAX_CHANNELS). */
export const MAX_VISIBLE_CHANNELS = MAX_CHANNELS;

/** Brightfield consumes three channel slots (R, G, B). */
export const BRIGHTFIELD_CHANNEL_COUNT = 3;
