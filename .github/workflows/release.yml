name: Release
on:
  workflow_call:
    outputs:
      version:
        description: "New version"
        value: ${{ jobs.release.outputs.new_release_version }}

# HUSKY=0 keeps `npm ci` from installing the pre-push hook into
# `.git/hooks` / `core.hookspath`. Without this, semantic-release's
# tag push fires `.husky/pre-push` which runs `npx vitest run` — and
# the release job has neither `npx prisma generate` (so `~/.generated/
# client` is missing) nor TZ=Europe/Berlin (so the formatHumanReadableDate
# test fails 12:00 vs 13:00). Tests block the push, the tag never lands.
# qa-checks.yml and container.yml already set HUSKY=0 for the same reason.
env:
  HUSKY: 0

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write # publish GitHub release
      issues: write # comment on released issues
      pull-requests: write # comment on released PRs
      packages: write # npm
    outputs:
      new_release_published: ${{ steps.semantic.outputs.new_release_published }}
      new_release_version: ${{ steps.semantic.outputs.new_release_version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Semantic Release
        id: semantic
        uses: cycjimmy/semantic-release-action@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: package-json
          path: |
            package.json
            package-lock.json
          retention-days: 7

      - name: Generate Summary
        if: always()
        run: |
          echo "## Release Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Status**: ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.semantic.outputs.new_release_published }}" == "true" ]; then
            echo "- **Release Published**: ✓ Yes" >> $GITHUB_STEP_SUMMARY
            echo "- **Version**: ${{ steps.semantic.outputs.new_release_version }}" >> $GITHUB_STEP_SUMMARY
            echo "- **Release URL**: https://github.com/${{ github.repository }}/releases/tag/v${{ steps.semantic.outputs.new_release_version }}" >> $GITHUB_STEP_SUMMARY
          else
            echo "- **Release Published**: ✗ No (no changes warranting release)" >> $GITHUB_STEP_SUMMARY
          fi
